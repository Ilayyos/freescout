{{-- $TITLE_NAME$ Dashboard Widget --}}

<div class="panel panel-default $LOWER_NAME$-widget">
    <div class="panel-heading">
        <i class="glyphicon glyphicon-stats"></i>
        {{ __('$TITLE_NAME$') }}
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-xs-6">
                <div class="text-center">
                    <h2 class="margin-0">{{ $count ?? 0 }}</h2>
                    <p class="text-muted margin-0">{{ __('Total Items') }}</p>
                </div>
            </div>
            <div class="col-xs-6">
                <div class="text-center">
                    <h2 class="margin-0">{{ count($items ?? []) }}</h2>
                    <p class="text-muted margin-0">{{ __('Recent') }}</p>
                </div>
            </div>
        </div>

        @if(!empty($items))
            <hr>
            <ul class="list-unstyled margin-0">
                @foreach($items as $item)
                    <li class="margin-bottom-5">
                        <i class="glyphicon glyphicon-record text-muted"></i>
                        {{ $item->name ?? 'Item' }}
                    </li>
                @endforeach
            </ul>
        @endif
    </div>
    <div class="panel-footer text-center">
        <a href="{{ route('$LOWER_NAME$.index') }}">
            {{ __('View All') }} <i class="glyphicon glyphicon-arrow-right"></i>
        </a>
    </div>
</div>
