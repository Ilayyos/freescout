@extends('layouts.app')

@section('title', __('$TITLE_NAME$'))

@section('content')
    @include('partials/flash_messages')

    <div class="page-title">
        <div class="pull-right">
            <a href="{{ route('$LOWER_NAME$.create') }}" class="btn btn-primary">
                <i class="glyphicon glyphicon-plus"></i> {{ __('Create New') }}
            </a>
        </div>
        <h1>{{ __('$TITLE_NAME$') }}</h1>
    </div>

    {{-- Search --}}
    <div class="row margin-bottom">
        <div class="col-md-6">
            <form method="GET" action="{{ route('$LOWER_NAME$.index') }}" class="form-inline">
                <div class="input-group">
                    <input type="text" name="search" class="form-control"
                           placeholder="{{ __('Search...') }}"
                           value="{{ $search ?? '' }}">
                    <span class="input-group-btn">
                        <button class="btn btn-default" type="submit">
                            <i class="glyphicon glyphicon-search"></i>
                        </button>
                        @if(!empty($search))
                            <a href="{{ route('$LOWER_NAME$.index') }}" class="btn btn-default">
                                <i class="glyphicon glyphicon-remove"></i>
                            </a>
                        @endif
                    </span>
                </div>
            </form>
        </div>
    </div>

    {{-- Data Table --}}
    <div class="panel panel-default">
        <div class="panel-body p-0">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            $TABLE_HEADERS$
                            <th>{{ __('Created') }}</th>
                            <th class="text-right">{{ __('Actions') }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse ($items as $item)
                            <tr>
                                $TABLE_CELLS$
                                <td>{{ $item->created_at->format('M j, Y') }}</td>
                                <td class="text-right">
                                    <a href="{{ route('$LOWER_NAME$.edit', $item->id) }}"
                                       class="btn btn-xs btn-default">
                                        {{ __('Edit') }}
                                    </a>
                                </td>
                            </tr>
                        @empty
                            <tr>
                                <td colspan="10" class="text-center text-muted">
                                    {{ __('No items found.') }}
                                </td>
                            </tr>
                        @endforelse
                    </tbody>
                </table>
            </div>
        </div>
        @if ($items->hasPages())
            <div class="panel-footer text-center">
                {{ $items->appends(request()->query())->links() }}
            </div>
        @endif
    </div>
@endsection
